import { Palette } from '../styled';

interface Theme {
  color: Palette;
}

const theme: Theme = {
  color: {
    primary: '#0376be',
    primaryDark: '#1d3557',
    primaryLight: '#a8dadc',
    white: '#ffffff',
  },
};

export default theme;
