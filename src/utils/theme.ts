import { ColorPalette } from '../styled';

interface Theme {
  color: ColorPalette;
}

const theme: Theme = {
  color: {
    primary: '#0376be',
    primaryDark: '#1d3557',
    primaryLight: '#a8dadc',
    white: '#ffffff',
  },
};

export default theme;
